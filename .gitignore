# Sensitive files — never commit these
.env
*.env.*
secrets/
credentials/
*.key
*.pem
*.crt

# Config files that may contain local paths or passwords
smb.conf.local
docker-compose.override.yml

# OS-generated files
Thumbs.db
.DS_Store
Desktop.ini

# Editor artifacts
*.swp
*.swo
*~
.vscode/settings.json

# Logs
*.log
logs/

# Temporary files
*.tmp
*.bak

# Archive files
*.zip
*.tar.gz
*.tar
*.gz

# Windows system files
desktop.ini
Desktop.ini
Thumbs.db

# Raw screenshot dumps — large unorganized batches, not for the repo
raw-screenshots/

# Screenshot working folders — originals and intermediate edits
# ** prefix matches at any depth, so this covers:
#   screenshots/Originals/
#   homelab/pihole-dns-filtering-lab/screenshots/Originals/
#   any future lab/screenshots/Originals/
**/screenshots/Originals/
**/screenshots/edited/

# screenshots/final/ is intentionally NOT listed here — keep it tracked

# Resume binary files — old versions, PDFs, Word docs
# Keep only trea_resume_master.md (the source of truth)
resume/*.pdf
resume/*.docx
resume/desktop.ini
resume/Trea_Kelly_Remote_Level1_MSP_HelpDesk.md

# Private notes — interview prep, study material, internal planning docs
_private-notes/
interview-notes/

# Profile README lives in tkellz30/tkellz30 — not part of this portfolio repo
github-readmes/
